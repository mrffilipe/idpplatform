# Copy to docker/.env.infrastructure and adjust.
# Used by: docker compose -f docker/docker-compose.infrastructure.yml --env-file docker/.env.infrastructure up -d

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword
POSTGRES_DB=idpplatform_db
POSTGRES_PORT=5432

REDIS_PASSWORD=default_password
REDIS_PORT=6379
